<%= page_title("#{t 'resellers.dashboard.title'}") do %>
  <%= button_link_to "#{t 'resellers.orders.new', default: 'New Order'}", new_reseller_order_path, class: 'btn-default pull-right' %>
<% end %>