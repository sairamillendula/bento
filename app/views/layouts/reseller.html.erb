<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow">
    <title><%= t('theme.site_name') %> | Reseller</title>
    <%= stylesheet_link_tag    "application", "admin", :media => "all" %>
    <%= javascript_include_tag "https://js.stripe.com/v1/" if @checkout_script%>
    <%= javascript_include_tag "admin" %>
    <%= csrf_meta_tags %>
    <%= tag :meta, name: "stripe-key", content: ENV['STRIPE_PUBLIC_KEY'] if @checkout_script %>
    <script type="text/javascript">
      CURRENT_LOCALE = '<%= I18n.locale %>';
    </script>
  </head>

  <body class="admin">
    <section id="sidebar">
      <%= render 'reseller_sidebar' %>
    </section>
    <div id="wrapper">
      <header id="header">
        <%= render 'reseller_navbar' %>
      </header>
      <section id="content" class="row-fluid">
        <%= render 'flash' %>
        <%= yield %>
      </section>
    </div>

  </body>
</html>