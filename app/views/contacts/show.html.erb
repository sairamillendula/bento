<%= page_title("#{t 'theme.contact.title'}") %>

<%= simple_form_for @contact, url: contact_path do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :content, label: "Message", as: :text %>
  <%= submit_tag "#{t 'theme.contact.submit'}" %>
<% end %>