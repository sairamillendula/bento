<h1>Contact Us</h1>

<%= simple_form_for @contact, url: contact_path do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :content, as: :text %>
  <%= submit_tag 'Send Message' %>
<% end %>