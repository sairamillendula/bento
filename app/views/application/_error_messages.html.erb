<% if object.errors.any? %>
 <div class="alert alert-error">
    <p>Some errors were found, please take a look:</p>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>